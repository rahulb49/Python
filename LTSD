
n = int(input("How many?"))

for i in range(n):
    list_user = list(input("enter name"))
    copy_list = list_user.copy()
    copy_list.reverse()
    if (list_user == copy_list):
        print("palidrome")
    else:
        print("not Palidrome")
print(type(list_user))

=-----------------------TUPLE____________________
tuple= ("c","d","a","b","a","a")

print(tuple.count("a"))

list1 = list(tuple)
print (type(list1))
list1.sort()
print(list1)


print(sorted(list1))
